Create table snowboard (id int(6) PRIMARY KEY AUTO_INCREMENT NOT NULL,
                    boardtpe varchar(255),
                    brand varchar(255),
                    msrp varchar(255),
                    size varchar(255)
                    )
